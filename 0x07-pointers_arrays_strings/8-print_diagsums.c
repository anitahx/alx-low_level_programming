#include "main.h"
#include <stdio.h>

/**
 * print_diagsums -  prints the sum of the two diagonals of a square matrix of integers.
 * Return: sum of diagonals of a square of matrix of integers
 */

void print_diagsums(int *a, int size)

